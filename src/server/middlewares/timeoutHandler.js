////////////////////////////////////////////////////////////////////////////////////////////
export default ( req, res, next ) => {

    req.on('timeout',_=> res.status(503).json({message:'Timeout'}) )
    next()
}
////////////////////////////////////////////////////////////////////////////////////////////